# 服务器端口 | Server Port
PORT=3005

# 是否启用访问密码保护（默认为 false）
# Whether to enable password protection (default: false)
# 设置为 true 启用密码保护，保护你的脚本不被泄露
# Set to true to enable password protection and secure your scripts
ENABLE_AUTH=true

# 访问密码（当 ENABLE_AUTH=true 时生效）
# Access password (effective when ENABLE_AUTH=true)
ACCESS_PASSWORD=admin123

# Session 密钥（生产环境请修改）
# Session secret key (please change in production)
SESSION_SECRET=subconverter-secret-key-change-in-production
ENABLE_LOG=true
# 日志文件路径（默认 logs/server.log），可自定义绝对或相对路径
LOG_FILE=logs/server.log

# 文件日志格式：jsonl（默认，更详细）或 text（与控制台一致的纯文本）
LOG_FILE_MODE=jsonl

# 当 LOG_FILE 不可写时，是否自动降级写入 /tmp/subconverter.log
# true（默认）：保证尽量有落盘日志，但可能出现“额外日志文件”
# false：严格只写 LOG_FILE（不可写则不写文件日志，不会创建 /tmp 日志）
LOG_FALLBACK=true

# 可选：额外镜像一份日志到另一个文件（例如 /var/log/...）
# LOG_MIRROR_FILE=/var/log/subconverter/server.log
